server:
  port: 5001
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user_service
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
management:
    endpoints:
        web:
            exposure:
                include: "*"