server:
  port: 5003
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/category_service
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
management:
    endpoints:
        web:
            exposure:
                include: "*"